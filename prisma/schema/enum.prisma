//=============================== User related enum ==================//
enum UserStatus {
    ACTIVE   @map("active")
    INACTIVE @map("inactive")
    BLOCKED  @map("blocked")
    BANNED   @map("banned")
}

enum UserRole {
    SUPER_ADMIN @map("super_admin")
    ADMIN       @map("admin")
    CR          @map("cr")
    STUDENT     @map("student")
}

//============================= Institution Model ====================/

enum InstitutionType {
    COLLEGE     @map("college")
    POLYTECHNIC @map("polytechnic")
    UNIVERSITY  @map("university")
}

//============================ CRRegistration  Model Related enum ==============//
enum CRRegistrationStatus {
    PENDING  @map("pending")
    APPROVED @map("approved")
    REJECTED @map("rejected")
}

// ================================= Session Model Enum ================//
enum SessionType {
    SEMESTER @map("semester")
    YEAR     @map("year")
}
